<Window x:Class="TiclyMusic.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - Ticly Music" Height="420" Width="420" 
        Background="#e9e9e9" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="8" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#565656" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Ticly Music Settings" FontSize="18" FontWeight="Bold" 
                      Foreground="#565656" Margin="0,0,0,20" HorizontalAlignment="Center" />

            <!-- Spotify Section -->
            <GroupBox Header="Spotify API Configuration" Margin="0,0,0,15" Padding="10">
                <StackPanel>
                    <TextBlock Text="Client ID:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#565656" />
                    <TextBox x:Name="SpotifyClientIdTextBox" Height="30" Margin="0,0,0,10"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             ToolTip="Enter your Spotify Client ID" />
                    <TextBlock Text="Client Secret:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#565656" />
                    <TextBox x:Name="SpotifyClientSecretTextBox" Height="30" Margin="0,0,0,10"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             ToolTip="Enter your Spotify Client Secret" />
                    <TextBlock Text="Create a Spotify app to get Client ID/Secret" 
                              FontSize="10" Foreground="#888" TextWrapping="Wrap" Margin="0,0,0,10" />
                    <Button Content="Open Spotify Dev Guide" Click="SpotifyGuide_Click" 
                           Background="#2196F3" Foreground="White" Height="30" Width="170"
                           Style="{StaticResource RoundedButtonStyle}" HorizontalAlignment="Left" />
                </StackPanel>
            </GroupBox>

            <!-- SpotiFLAC Section -->
            <GroupBox Header="SpotiFLAC Backend" Margin="0,0,0,15" Padding="10">
                <StackPanel>
                    <TextBlock Text="Base URL:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#565656" />
                    <TextBox x:Name="SpotiFlacBaseUrlTextBox" Height="30" Margin="0,0,0,10"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             ToolTip="Enter your SpotiFLAC backend base URL" />
                    <TextBlock Text="API Key (optional):" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#565656" />
                    <TextBox x:Name="SpotiFlacApiKeyTextBox" Height="30" Margin="0,0,0,10"
                             Style="{StaticResource RoundedTextBoxStyle}"
                             ToolTip="Optional API key for SpotiFLAC backend" />
                    <TextBlock Text="SpotiFLAC backend should expose /stream and /lyrics endpoints" 
                              FontSize="10" Foreground="#888" TextWrapping="Wrap" />
                </StackPanel>
            </GroupBox>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Save" Width="80" Height="35" Click="SaveButton_Click"
                        Background="#4CAF50" Foreground="White" Margin="0,0,10,0"
                        Style="{StaticResource RoundedButtonStyle}" />
                <Button Content="Cancel" Width="80" Height="35" Click="CancelButton_Click"
                        Background="#F44336" Foreground="White"
                        Style="{StaticResource RoundedButtonStyle}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>